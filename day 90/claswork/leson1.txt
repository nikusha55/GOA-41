# 1. 
# 1) კოლექციის ტიპები და მათი თვისებები:
# List (სია)
# თვისებები:
# Mutable (შესაცვლელია): შეგიძლიათ ელემენტების შეცვლა, დამატება და წაშლა.
# ელემენტები ინახება რიგის დაცვით (ordered).
# შესაძლებელია ელემენტების დუბლირება.
# შეიცავს ნებისმიერი ტიპის მონაცემებს.
# Tuple (ტუპლი)
# თვისებები:
# Immutable (უცვლელია): ელემენტების დამატება, წაშლა ან შეცვლა შეუძლებელია.
# ელემენტები ინახება რიგის დაცვით (ordered).
# შესაძლებელია ელემენტების დუბლირება.
# შეიცავს ნებისმიერი ტიპის მონაცემებს.
# Set (მრავალი)
# თვისებები:
# Mutable (შესაცვლელია): ელემენტების დამატება და წაშლა შესაძლებელია.
# ელემენტების რიგი არ არის შენახული (unordered).
# დუბლირებულ ელემენტებს არ ინახავს (უნიკალური მნიშვნელობები).
# შეიცავს მხოლოდ იმ ელემენტებს, რომლებიც hashable-ია (მაგ., list ან set არ შეიძლება იყოს ელემენტი).


# 2.
# List
# list1 = [1, 2, 3, 4, "apple", True]

# # Tuple
# tuple1 = (1, 2, 3, 4, "banana", False)

# # Set
# set1 = {1, 2, 3, 4, "cherry"}

# 3.
# # List
# list1 = [1, 2, 3, 4, "apple", True]

# # Tuple
# tuple1 = (1, 2, 3, 4, "banana", False)

# # Set
# set1 = {1, 2, 3, 4, "cherry"}
